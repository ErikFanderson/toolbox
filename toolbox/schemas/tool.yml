"include('tool_schema', strict=True)"
---
tool_schema:
  tool: "str()"
  namespace: "str()"
  properties: "map(include('property'))"
  schema_includes: "map(required=False)"
---
property:
  default: "anything()"
  description: "str()"
  schema: "str()"
